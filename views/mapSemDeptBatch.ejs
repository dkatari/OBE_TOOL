<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Map Semester, Department, and Batch</title>
</head>

<body>
    <h1>Map Semester, Department, and Batch</h1>
    <p>Selected Batch: <%= batch.batch_start_year %> - <%= batch.batch_end_year %>
    </p>

    <form action="/map-sem-dept-batch/submit" method="POST">
        <input type="hidden" name="batch_id" value="<%= batch.batch_id %>">

        <label for="sem_number">Semester:</label>
        <input type="number" name="sem_number" id="sem_number" required min="1" max="8">
        <br>

        <label for="dept_id">Department:</label>
        <select name="dept_id" id="dept_id" required>
            <% departments.forEach(dept=> { %>
                <option value="<%= dept.dept_id %>">
                    <%= dept.dept_name %>
                </option>
                <% }); %>
        </select>
        <br>

        <label for="start_date">Semester Start Date:</label>
        <input type="date" name="start_date" id="start_date" required>
        <br>

        <label for="end_date">Semester End Date:</label>
        <input type="date" name="end_date" id="end_date" required>
        <br>

        <button type="submit">Save Mapping</button>
    </form>
</body>

</html>